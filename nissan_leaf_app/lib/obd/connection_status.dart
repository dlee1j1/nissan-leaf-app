/// Connection status enum - reported out by BluetoothDeviceManager.dart
enum ConnectionStatus {
  disconnected,
  scanning,
  scanComplete,
  connecting,
  connected,
  ready,
  disconnecting,
  error,
}
